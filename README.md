Script to fetch [mahakim.ma](https://mahakim.ma) jurisdiction and types and write it to a json file

-----

### How to use

- Clone the project with `git clone`
- Run `npm install`
- Run `node index.js`
